// Vector settings
//
// For information on how to configure Vector, see:
// docs/src/configuring-vector.md
//
// To see all of Vector's default settings without changing your
// custom settings, run `vector: open default settings` from the
// command palette (cmd-shift-p / ctrl-shift-p)
{
  "ui_font_size": 16,
  "buffer_font_size": 16,
  "theme": {
    "mode": "system",
    "light": "One Light",
    "dark": "One Dark"
  }
}

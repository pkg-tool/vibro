// Vector keymap
//
// For information on binding keys, see the Vector documentation:
// https://https://github.com/pkg-tool/vector/tree/master/docs/key-bindings
//
// To see the default key bindings run `vector: open default keymap`
// from the command palette.
[
  {
    "context": "Workspace",
    "bindings": {
      // "shift shift": "file_finder::Toggle"
    }
  },
  {
    "context": "Editor",
    "bindings": {
      // "j k": ["workspace::SendKeystrokes", "escape"]
    }
  }
]
